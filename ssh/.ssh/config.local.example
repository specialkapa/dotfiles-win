# Work-specific SSH hosts — copy to ~/.ssh/config.local (gitignored) and edit.
# Included by ~/.ssh/config via "Include config.local".
Host az-ghb-vm
  HostName localhost
  Port 2022
  User youradminuser
