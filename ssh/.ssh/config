# Personal SSH config.
# Work-specific hosts (e.g. the TNP Bastion tunnel) live in ~/.ssh/config.local,
# which is gitignored — copy config.local.example to create it.
Include config.local

Host 127.0.0.1
  HostName 127.0.0.1
